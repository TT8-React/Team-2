import React from 'react'
import { Btn } from './style';
const FormBtn = ({ name }) => {
    return ( <
        >
        <
        Btn type = 'submit' > { name } < /Btn> <
        />
    );
}

export default FormBtn